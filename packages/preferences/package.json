{
  "name": "@phantompane/preferences",
  "version": "6.2.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "oxfmt --check . && oxlint .",
    "fix": "oxfmt --write . && oxlint --fix .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root ../.. packages/preferences/src"
  },
  "dependencies": {
    "@phantompane/git": "workspace:*",
    "zod": "^4.3.6"
  }
}
