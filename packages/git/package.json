{
  "name": "@phantompane/git",
  "version": "6.3.0-6",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "oxfmt --check . && oxlint .",
    "fix": "oxfmt --write . && oxlint --fix .",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run --root ../.. packages/git/src"
  },
  "dependencies": {
    "@phantompane/process": "workspace:*",
    "@phantompane/utils": "workspace:*"
  }
}
