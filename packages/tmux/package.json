{
  "name": "@phantompane/tmux",
  "version": "6.1.1-1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "oxfmt --check . && oxlint .",
    "fix": "oxfmt --write . && oxlint --fix .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root ../.. packages/tmux/src"
  },
  "dependencies": {
    "@phantompane/process": "workspace:*",
    "@phantompane/shared": "workspace:*"
  }
}
