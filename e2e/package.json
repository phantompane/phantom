{
  "name": "@phantompane/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxfmt --check . && oxlint .",
    "fix": "oxfmt --write . && oxlint --fix .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "version": "0.1.0-0"
}
